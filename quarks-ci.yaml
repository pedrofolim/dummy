agent: any

stages:
    - name: Build
      agent: any
      steps:
        - echo: "Build ok"
    - name: Push
      agent: any
      steps:
        - echo: "Build ok"
    - name: Test
      agent: any
      steps:
        - echo: "Test ok"
    - name: Deploy
      agent:
        label: Deploy
        customWorkspace: /tmp
        dockerImage: "gcr.io/sysmap-quarks/quarks/voyager/builders/deploy:1.0.3"
        args: -deploy
      steps: 
        - echo: "Hello from stage2"
configuration:
    archiveArtifacts:
        - quarks-ci.yaml